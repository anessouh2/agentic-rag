# ── Core LLM framework ──────────────────────────────────────────
langchain>=0.2.0                # main LangChain orchestration library
langchain-mistralai>=0.1.0      # Mistral AI chat model + embeddings integration
langchain-community>=0.2.0      # community loaders (PyPDFLoader, Chroma, etc.)

# ── Vector store ────────────────────────────────────────────────
chromadb>=0.5.0                 # local vector database for cosine-similarity search

# ── Graph orchestration ────────────────────────────────────────
langgraph>=0.1.0                # LangGraph state-machine for multi-agent workflows

# ── PDF processing ──────────────────────────────────────────────
pypdf>=4.0.0                    # PDF text extraction (used by PyPDFLoader)

# ── Structured output ──────────────────────────────────────────
pydantic>=2.0.0                 # data validation / structured LLM output schemas

# ── Environment ─────────────────────────────────────────────────
python-dotenv>=1.0.0            # loads .env file into os.environ
